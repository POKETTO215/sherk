# Disturbed Text Animation (p5.js)

- 全屏自适应文字扰动动画，适配移动端和PC
- 支持 Github Pages 部署
- 如需自定义字体，请在 `preload()` 加载 `.ttf` 并替换 `textFont()` 调用

## 部署
1. 克隆本仓库到你的 GitHub
2. 在仓库设置启用 GitHub Pages（分支选 `main`，目录选 `/ (root)`）
3. 访问 `https://你的用户名.github.io/仓库名/`

如需自定义文本或参数，修改 `sketch.js` 即可。
